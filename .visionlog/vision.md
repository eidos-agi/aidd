---
title: "aidd — the evals are the company"
type: "vision"
date: "2026-08-23"
---

AI made rebuilds cheap. Semantic parity is still expensive. AIDD is the operating model: a public champion, versioned generation repos, frozen evals, evidence-gated promotion.

The generation is `product-vN`. The public name stays `product`. AI bashes in the generation. Winners get pushed back. The public sees the upgrade. The org keeps the competition.

This repo is knowledge. Skills, doctrine, templates, the oracle. Not a runtime. A product later owns a binary.

Implementation is disposable. Intent, oracles, and fitness are not. Verifiability sets the automation ceiling. Taste stays human.
