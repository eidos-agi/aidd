# /aidd-next

Autonomous Next.js App Router + shadcn/ui. Playwright is the gate. Not iPhone.

1. Read `plugins/nextjs-shadcn/README.md`, `doctrine/app-router.md`, and `doctrine/shadcn.md`.
2. Confirm stable test ids, mocks, deterministic clock, resettable state. Do not poke hashed classes.
3. Mechanical gates first. Hydration, axe, Radix dialogs, labelled forms, 390 and 1280, light and dark portals.
4. Run Playwright against `next build && next start`. `next dev` is not the release gate.
5. Explorer under `explorer/mission.yaml`. Convert traces to fixtures.
6. Critic on web evidence packets only. A native Apple pass is inadmissible. Fail dashboard slop.
7. Constrained fixes. New `components/ui` primitives need a contract update.
8. Humans only for new interaction models, primitives, taste, and promotion.

The builder does not approve its own UI.
