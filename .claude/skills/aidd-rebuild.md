# /aidd-rebuild

Open or continue `product-vN`. Do not bash in `product`.

1. Confirm a trigger in `doctrine/rebuild-triggers.md`. Calendars are not triggers.
2. Create or continue `github.com/<org>/<product>-vN`. Write `GENERATION.md`.
3. Copy evals forward from the current latest (`product`). Add fixtures. Do not weaken what arrived.
4. Build from the thesis. The last winner is an oracle for behavior, not a template for internals.
5. Run public contracts. Do not peek at held-out fixtures.
6. Convert every novel failure into a fixture before asking for `vN+1`.

Time-box. A vertical slice. If it cannot beat what is currently in `product`, it does not get copied back. Leave the generation repo as evidence.
