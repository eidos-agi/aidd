# Constrained fix — iPhone

Allowed: reorder for thumb reach, use existing compact primitives, sheets, stacks, tab items, safe-area padding, copy under the voice guide.

Requires a contract update: a sidebar, a split view, a new root chrome, a pointer-only control, a new navigation model.

Do not "fix" an iPhone miss by shipping the iPad layout.
