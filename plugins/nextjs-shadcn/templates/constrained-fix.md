# Constrained fix — Next.js + shadcn

Allowed: compose existing `components/ui/*`, retoken via CSS variables, split a client island smaller, add `loading.tsx` / `error.tsx`, bind labels, move a primary Button, copy under the voice guide.

Requires a contract update: a new `components/ui` primitive, a new color role, mixing New York into Default, a whole-page client wrapper, a new navigation chrome.

Do not "fix" a web miss by screenshot-tuning hashed classes. Do not ship a native iPhone layout in the browser as a substitute for 390.
