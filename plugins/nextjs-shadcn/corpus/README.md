# Corpus — Next.js + shadcn

Approved web screens only. Native iPhone screenshots are the wrong corpus.

Keep 390 and 1280, light and dark, a dialog open, a sheet open on 390, an empty state, a validation error.

Held-out copies live in `plugin-evals/nextjs-shadcn/`.
