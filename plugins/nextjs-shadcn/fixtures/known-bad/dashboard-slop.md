# FIXTURE known-bad / dashboard-slop

gates:
  - no-slop-surface
  - one-primary-action
must_fail:
  - critic
  - product-design

Eight gradient Cards, three filled Buttons, a badge on every row. No primary task in the first viewport.

Expected catch: this is the AI web look. The constitution forbids it unless the screen contract explicitly allows a metric wall.
