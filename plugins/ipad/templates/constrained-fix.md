# Constrained fix — iPad

Allowed: introduce sidebar+detail using existing primitives, popover inspectors, reflow at Split View ratios, pointer hover as enhancement, hardware-keyboard shortcuts, copy under the voice guide.

Requires a contract update: a new root chrome, a Mac-only menu, dropping touch, a new multi-window model.

Do not "fix" an iPad miss by shipping the iPhone layout larger.
