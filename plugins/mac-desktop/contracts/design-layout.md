# design/ in the product repo

The plugin's constitution lives next to the app, not only in this forge.

```
design/
  principles.md
  voice-and-copy.md
  tokens.json
  components/
    AppButton.md
    AppForm.md
    AppList.md
    AppEmptyState.md
  contracts/
    global.yaml
    onboarding.yaml
    account.yaml
  references/
    approved/
    anti-patterns/
  evaluations/
    critic-rubric.json
    calibration-set.jsonl
```

Copy `contracts/global.yaml` and `contracts/screen.yaml` as starting points. Fill them. A screen without a job cannot be inspected.
