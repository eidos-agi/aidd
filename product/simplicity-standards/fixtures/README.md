# fixtures

Minimized examples of unearned surface. Not the current champion's entire backlog.

When a prune is rejected because a hidden job existed:

1. Add that job to `ai-users/` first.
2. Then keep or kill with evidence.

When a prune is accepted:

1. Keep the deletion proposal.
2. Keep a tombstone if a public contract existed.
3. Put a held-out "this must stay gone" fixture in product-evals.
